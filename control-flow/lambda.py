# A lambda is a small anonymous function.

# Small → usually one line.
# Anonymous → it has no name.


add = lambda a, b: a + b

print(add(2, 3))


# syntax
# lambda parameters: expression

# why use lambda

# Suppose you need a function only once


# Limitation
# Lambda can contain only one expression.